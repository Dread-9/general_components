export interface IonicComponent {
    id: string;  // Identificador único para el componente
    url: string; // URL asociada al componente
    title: string; // Título del componente
    img:string
    description: string; // Descripción del componente
}
